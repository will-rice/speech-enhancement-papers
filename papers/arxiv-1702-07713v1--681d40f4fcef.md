---
identifier: arxiv:1702.07713v1
title: Multichannel Linear Prediction for Blind Reverberant Audio Source Separation
authors:
  - İlker Bayram
  - Savaşkan Bulek
published: "2017-02-24T17:23:01+00:00"
url: https://arxiv.org/abs/1702.07713v1
source: arxiv
doi: null
arxiv_id: 1702.07713v1
categories:
  - cs.CE
  - cs.SD
---

# Multichannel Linear Prediction for Blind Reverberant Audio Source SeparationThanks: İ. Bayram is with the Dept. of Electronics and Communications Eng., Istanbul Technical University, Istanbul, Turkey. E-mail : ibayram@itu.edu.tr. S. Bulek is with Qualcomm Atheros, Inc., Auburn Hills, MI, USA. E-mail : sbulek@gmail.com.

İlker Bayram    Savaşkan Bulek Affiliation: 

###### Abstract

A class of methods based on multichannel linear prediction (MCLP) can
achieve effective blind _dereverberation_ of a source, when the source
is observed with a microphone array. We propose an inventive use of MCLP
as a pre-processing step for blind _source separation_ with a microphone
array. We show theoretically that, under certain assumptions, such
pre-processing reduces the original blind reverberant source separation
problem to a non-reverberant one, which in turn can be effectively
tackled using existing methods. We demonstrate our claims using real
recordings obtained with an eight-microphone circular array in
reverberant environments.

###### Index Terms: 

Microphone array, source separation, blind dereverberation, multichannel
linear prediction, beamforming, post-filtering.

## I Introduction

In anechoic environments, audio source separation with a microphone
array can be achieved by beamforming and post-filtering
\[[7](#bib.bib7)\]. On the other hand, in reverberant environments,
reverberation causes significant degradation in the quality and
intelligibility of the beamformer output, especially if the room impulse
responses (RIR) are not taken into account or are not known precisely.
This is a serious limitation because in practice, RIRs are usually not
known with precision and they can vary wildly with respect to the
positions of the microphone/source pair.

A related problem addressing reverberation without explicit RIR
knowledge is called blind dereverberation \[[17](#bib.bib17)\]. A
successful class of methods for blind dereverberation fall under the
framework of multi-channel linear prediction (MCLP) \[[16](#bib.bib16),
[11](#bib.bib11), [10](#bib.bib10)\]. These methods require a microphone
array and employ linear prediction to extract information about the
properties of reverberation. In this paper, we propose to employ MCLP,
which is primarily used for single source dereverberation, as a
pre-processing step in source separation. We show theoretically and
demonstrate numerically that such a scheme not only dereverberates the
sources but also helps in the subsequent separation.

In order to make this statement more precise, let us briefly discuss an
MCLP formulation. Consider an array of microphones, recording a source
in a reverberant room. Recent MCLP formulations \[[16](#bib.bib16),
[11](#bib.bib11), [10](#bib.bib10)\] select one of the microphones as
the reference and use delayed versions of the whole set of observations
to linearly predict the reference signal. The residual of prediction,
which may also be interpreted as the ‘innovations’ component, forms the
dereverberated estimate of the source. In our scenario, we have multiple
sources recorded with a microphone array and we would like to separate
and dereverberate the sources. For this, suppose we apply MCLP for each
possible choice of the reference microphone. If we have $`N`$
 microphones in the array, this results in $`N`$ hopefully dereverberant
but mixed observations. We say ‘hopefully dereverberant’ because the use
of MCLP has previously been considered and justified solely for the case
of a single source \[[11](#bib.bib11)\]. Further, even if the MCLP
outputs thus obtained are dereverberated, in order to achieve
separation, we have to know the relation between the recordings. In this
paper, we show that, under a mild assumption on the RIRs, MCLP actually
suppresses reflections other than the direct source and preserves the
phase of the direct source. Therefore, the MCLP preprocessing step
essentially reduces the reverberant separation problem to an anechoic
one. This greatly simplifies the separation step because we can now use
the known array geometry to estimate the direction of arrival and
perform separation using this estimate. We argue that this effect is not
dependent on the following DOA estimation and separation algorithms. In
that sense, the proposed MCLP stage complements currently available
source separation algorithms, improving their performance. Consequently,
we do not have any reservations about which method to use following the
MCLP stage. Nevertheless, for the sake of completeness, we include a
brief description of the geometric source separation (GSS) algorithm
\[[18](#bib.bib18)\] followed by a post-filter.

### Related Work

Literature on dereverberation/source separation with a microphone array
is vast. Here, we focus on blind ¹¹ 1 We use the term ‘blind’ to refer
to methods that do not require to know the RIRs. However, the blind
methods we consider may use the known array geometry, which is
independent of the characteristics of the environment. methods for
dereverberation and source separation for arrays with a known geometry.

For source separation, provided the directions of arrival (DOA) of the
sources are known, and reverberation is limited, beamformers like the
minimum variance distortionless response (MVDR) beamformer, along with a
post-filter \[[2](#bib.bib2), [7](#bib.bib7), [23](#bib.bib23)\] can be
effective. In MVDR, one aims to minimize the energy of the reconstructed
signals, while preserving the signals from the DOAs. However,
reverberation causes a source to contribute to different directions,
reducing the effectiveness of such approaches. Geometric source
separation (GSS) \[[18](#bib.bib18)\] addresses this problem by also
enforcing statistical independence of the reconstructed sources. This is
achieved by selecting the beamformer weights so as to minimize a cost
function that consists of the sum of a term imposing geometric
constraints based solely on the array geometry and a term that promotes
the statistical independence of the sources (motivated by blind source
separation techniques \[[9](#bib.bib9)\]). In practice, GSS outputs are
still reverberant for moderate to highly reverberant rooms. GSS has been
complemented with a post-filter in \[[28](#bib.bib28),
[27](#bib.bib27)\] to devise a high performance source-separation method
that can operate in real time. An alternative approach, based on the
assumption that the activity of the different sources do not overlap in
the time-frequency domain \[[31](#bib.bib31)\], has been proposed in
\[[24](#bib.bib24)\]. However, the model in \[[24](#bib.bib24)\] assumes
that the room impulse responses are relatively short, compared to the
analysis window size used in the STFT.

Despite its simplicity, blind dereverberation is a challenging problem
due mainly to the characteristics of room impulse responses. For a
general overview and discussion of relatively recent methods, we refer
to the edited book \[[17](#bib.bib17)\]. Here, we focus on a successful
class of methods that can be collected under the heading ‘multichannel
linear prediction’ (MCLP), as they are more directly related to the
content of our work. These methods usually fit in a schema as follows.
Given multiple observations $`y_{i}`$, for $`i=1,2,\ldots,M`$, one of
the observations is chosen as the reference. Then, this reference
observation is linearly predicted by the delayed version of all of the
observations. Once the linear prediction coefficients are determined,
this information has been used differently in prior work. For instance,
in \[[12](#bib.bib12)\], the authors use the coefficients to estimate
the power of late reverberations, which are subsequently reduced by
spectral subtraction. In more recent work \[[16](#bib.bib16),
[11](#bib.bib11), [10](#bib.bib10)\], the residual of linear prediction
is taken as the dereverbed audio signal. Prior work also differentiates
based on the criteria used for determining the weights used in linear
estimation. In \[[12](#bib.bib12)\], the authors propose to select the
linear estimation weights so as to minimize the energy of the residual.
In \[[16](#bib.bib16)\], the source signal is modeled as a time-varying
Gaussian process and the weights are selected so as to maximize the
likelihood of such a model. More recently, \[[11](#bib.bib11),
[10](#bib.bib10)\] employ a sparsity- based model in the time-frequency
domain and select the weights so that the residual minimizes a sparsity
promoting cost function.

An interesting approach that employs MCLP to achieve suppression of late
reverberation and source separation is presented in
\[[26](#bib.bib26)\]. The authors use MCLP to suppress late
reverberation, using a statistical framework similar to that in
\[[16](#bib.bib16)\]. In contrast to \[[16](#bib.bib16)\], a
time-varying scenario with multiple sources is considered and the MCLP
weights are determined along with the statistics of the late reverberant
part of the sources in an iterative manner.

### Contribution

Our proposal in this paper is of a complementary nature and is likely to
improve the performance of existing separation algorithms. We present an
inventive way to employ MCLP as a pre-processing step for blind source
separation using a microphone array. We show theoretically that, under
certain assumptions, such a pre-processing step converts the reverberant
source separation problem into a non-reverberant one, which in turn can
be solved by existing methods such as \[[24](#bib.bib24),
[18](#bib.bib18), [27](#bib.bib27)\] outlined above. Such an approach is
inherently different than previous uses of MCLP which aim to dereverb a
single source as in \[[12](#bib.bib12), [16](#bib.bib16),
[11](#bib.bib11), [10](#bib.bib10)\]. Our proposal also differs from
that of \[[26](#bib.bib26)\] because \[[26](#bib.bib26)\] requires the
source statistics to cancel the late reverberation and thus resorts to a
joint estimation of the linear prediction weights and source statistics.
This approach in \[[26](#bib.bib26)\] requires an iterative procedure
and is handled with an EM algorithm. In contrast, we propose to employ
the MCLP step once, in order to reduce the reverberant problem to an
approximately non-reverberant one.

In addition to theoretical justification, we demonstrate the validity of
the claims using real recordings. For the sake of completeness, we
include a description of GSS along with a simple post-filter, to be
applied following the MCLP step, in order to achieve separation.
However, after MCLP, other methods that assume short RIRs, or operate
under a free propagation model may also be used, as noted above.

### Notation

Sequences and continuous functions of time are denoted with a small
letter as in $`h(n)`$ or $`h(t)`$, respectively. The DFT and STFT of
sequences are denoted with a capital letter as $`H(\omega)`$ and
$`H(s,\omega)`$ respectively – which one is referred to will be clear
from the context. The $`z`$-transform of sequences are denoted with
calligraphic letters as in $`\mathcal{H}(z)`$. We refer to
$`\mathcal{H}(z)`$ as ‘causal’ if it is a polynomial of $`z^{-1}`$. For
a causal $`\mathcal{H}(z)`$, we denote the constant $`h(0)`$ also as
$`\mathcal{H}(\infty)`$.

Throughout the paper, $`K`$ denotes the number of sources and $`N`$
 denotes the number of microphones in the array.

## II Description of the Model

Throughout the paper, we are interested in far-field processing. Thus,
the distance of the source to the microphones is assumed to be
significantly greater than the array size. Our proposed
formulation/algorithm is in the STFT domain. However, for simplicity of
exposition, we first introduce a model in the continuous-time domain and
then present an approximation of this model in the STFT domain.

### II-A A Time-Domain Observation Model

We introduce the time-domain model in three stages, where complexity
increases with each stage. We start with the case of a single source,
single microphone. In the second stage, we consider the single source,
multiple microphone case. Finally, we discuss the multiple source,
multiple microphone case, which is the main scenario of interest in this
paper.

#### II-A1 Single Source, Single Microphone

For a given microphone-source pair, let $`x(t)`$ denote the signal
produced by the source. The observation $`y(t)`$ can be approximated as,

|     |     |     |     |
| --- | --- | --- | --- |
|     |

       ``` math
       y(t)\approx x(t)\ast\tilde{h}(t),
       ```                                |     | (1) |

where $`\tilde{h}(t)`$ is the room impulse response (RIR) for the given
source-microphone positions. Now suppose that the distance from the
source to the microphone is $`D`$ meters and the speed of sound in the
environment is $`c`$ m/sec. Then, the acoustic signal emitted by the
source reaches the microphone after a delay of $`D/c`$ seconds. We
declare the _distance compensated room impulse response_ (DCIR) to be

|     |     |     |     |
| --- | --- | --- | --- |
|     |

       ``` math
       h(t)=\tilde{h}\bigl(t+D/c\bigr).
       ```                               |     | (2) |

Notice that for practical purposes, we can work with the DCIR instead of
the RIR for a single microphone-source setup. For multiple microphones
or sources, we need to take into account relative distances, as
discussed next.

#### II-A2 Single Source, Multiple Microphones

Consider a setup consisting of a microphone array and a source as shown
in
Fig. [1](#S2.F1 "Fig. 1 ‣ II-A3 Multiple Sources, Multiple Microphones ‣ II-A A Time-Domain Observation Model ‣ II Description of the Model ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation").
Suppose we set the center of the array as the origin for a coordinate
system. For this origin, let the vector $`p_{i}`$ denote the position of
the the $`i^{\text{th}}`$ microphone. Also, let $`u_{x}`$ denote the
unit vector in the direction of the source. Finally, let
$`d_{i}=\langle p_{i},u_{x}\rangle`$. Note that $`d_{i}`$ denotes the
coordinate of the projection of the $`i^{\text{th}}`$ microphone’s
position to the subspace spanned by $`u_{x}`$. The induced time-delay is
$`\tau_{i}=-d_{i}/c`$. Notice that $`\tau_{i}`$ may be positive or
negative, i.e., may represent an advance or delay. Finally, let the DCIR
from the source to the $`i^{\text{th}}`$ microphone be denoted as
$`h_{i}(t)`$. Then, we model the observation at the $`i^{\text{th}}`$
microphone, namely $`y_{i}`$, as,

|     |     |     |     |
| --- | --- | --- | --- |
|     |

````math
y_{i}(t)\approx x(t)\ast h_{i}(t-\tau_{i})=x(t-\tau_{i})\ast h_{i}(t).
``` |  | (3) |

The far field assumption amounts to the following : If the distance of
the source to the center of the array is $`D`$, then the distance of the
source to the $`i^{\text{th}}`$ microphone is approximately $`D-d_{i}`$.
Under the far-field assumption, the described model preserves the
ability of the array to differentiate different directions. We next add
another layer to this model by considering multiple sources.

#### II-A3 Multiple Sources, Multiple Microphones

Suppose now that there are $`K`$ sources and $`N`$ microphones. Let the
$`k^{\text{th}}`$ source signal be denoted by $`x_{k}(t)`$. Also, let
the unit vector in the direction of the $`k^{\text{th}}`$ source be
denoted by $`u_{k}`$. For the $`i^{\text{th}}`$ microphone, positioned
at $`p_{i}`$, we set $`\tau_{i,k}=\langle p_{i},u_{k}\rangle/c`$.
Finally, let $`h_{i,k}(t)`$ denote the DCIR between the
$`k^{\text{th}}`$ source and the $`i^{\text{th}}`$ microphone. Then, we
model the observation at the $`i^{\text{th}}`$ microphone as,

|  |  |  |  |
|----|----|----|----|
|  |
``` math
y_{i}(t)\approx\sum_{k=1}^{K}x_{k}(t)\ast h_{i,k}(t-\tau_{i,k})=\sum_{k=1}^{K}x_{k}(t-\tau_{i,k})\ast h_{i,k}(t)
``` |  | (4) |

Notice that, the absolute delay from the $`k^{\text{th}}`$ source is not
included in the model. However, this is not a careless omission, because
this delay is in fact not known and is not estimated in practice.
Therefore, the described model is suitable for practical far-field
processing scenarios.

Fig. 1: Microphones and the source are denoted by $`m_{i}`$, and $`x`$
respectively. The center of the array, marked with a circle, is taken to
be the origin. The position vector for the $`i^{\text{th}}`$ microphone
is projected onto the space spanned by the unit vector pointing in the
direction of the source to obtain the relative coordinate $`d_{i}`$.
Note that $`d_{1}`$, $`d_{2}`$ (not shown) are positive whereas
$`d_{3}`$, $`d_{4}`$ (not shown) are negative.

### II-B An Approximate Model in the STFT Domain

We work in the STFT domain for reasons to be briefly clarified in the
sequel. We are specifically interested in the representation of an LTI
system in the STFT domain. Consider a convolution equation as,

|     |                     |     |     |
|-----|---------------------|-----|-----|
|     |
       ``` math
       y(n)=x(n)\ast h(n).
       ```                  |     | (5) |

Let $`Y(s,\omega)`$, $`X(s,\omega)`$ denote the STFTs of $`y`$ and $`x`$
respectively, obtained using a window $`w(n)`$. We differentiate between
two cases, following the discussion in \[[13](#bib.bib13)\] (see Lemma 1
in \[[13](#bib.bib13)\]).

1.  (I)
    If the support of $`h`$ is contained in the support of $`w`$, then

    |     |                                            |     |     |
    |-----|--------------------------------------------|-----|-----|
    |     |
           ``` math
           Y(s,\omega)\approx X(s,\omega)\,H(\omega),
           ```                                         |     | (6) |

    where $`H(\omega)`$ denotes the DFT of $`h`$.
2.  (II)
    If the support of $`h`$ is significantly larger than the support of
    $`w`$, then

    |  |  |  |  |
    |----|----|----|----|
    |  |
    ``` math
    Y(s,\omega)\approx X(s,\omega)\overset{s}{\ast}\tilde{H}(s,\omega),
    ``` |  | (7) |

    for some $`\tilde{H}(s,\omega)`$, where $`\overset{s}{\ast}`$
    denotes convolution with respect to the time variable $`s`$, defined
    as

    |  |  |  |  |
    |----|----|----|----|
    |  |
    ``` math
    X(s,\omega)\overset{s}{\ast}\tilde{H}(s,\omega)=\sum_{l}X(s-l,\omega)\,\tilde{H}(l,\omega).
    ``` |  | (8) |

    We remark that $`\tilde{H}(s,\omega)`$ is not necessarily the STFT
    of $`h`$, but is chosen so as to make the approximation as close as
    possible – see \[[20](#bib.bib20)\] for a discussion.

We remark that, as a special case of
([I](#S2.I1.i1 "item I ‣ II-B An Approximate Model in the STFT Domain ‣ II Description of the Model ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")),
if $`\tau`$ is small enough, then the STFT of $`x(n-\tau)`$ can be
approximated as $`X(s,\omega)\,\exp(-j\omega\,\tau)`$. Therefore, the
model introduced in
([4](#S2.E4 "In II-A3 Multiple Sources, Multiple Microphones ‣ II-A A Time-Domain Observation Model ‣ II Description of the Model ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"))
can be approximated in the STFT domain as

|  |  |  |  |
|----|----|----|----|
|  |
``` math
Y_{i}(s,\omega)\approx\sum_{k=1}^{K}\,X_{k}(s,\omega)\,\overset{s}{\ast}\,\Bigl(\tilde{H}_{i,k}(s,\omega)\,\exp(-j\omega\,\tau_{i,k})\Bigr).
``` |  | (9) |

Such a model is preferred over the time domain model because prior or
penalty functions that enforce (structured) sparsity of the audio
signals are typically defined in the STFT (or some other time-frequency)
domain. If we use the time domain convolutional model
([4](#S2.E4 "In II-A3 Multiple Sources, Multiple Microphones ‣ II-A A Time-Domain Observation Model ‣ II Description of the Model ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"))
in an iterative algorithm, we have to work in the time domain to undo
the effects of reverberation, and the STFT domain to take advantage of
the signal prior. Switching from the time domain representation to the
STFT representation at each iteration increases the computational cost
significantly. Working with a convolutional model directly in the STFT
domain, such as
([9](#S2.E9 "In II-B An Approximate Model in the STFT Domain ‣ II Description of the Model ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")),
allows to carry out the iterations in the STFT domain, eliminating the
need to apply the forward and inverse STFT operations at each iteration.

## III Multi-Channel Linear Prediction for Dereverberation

In this section, we discuss the implications of using multi-channel
linear prediction (MCLP) as a preliminary step for the source separation
problem. We derive conditions under which MCLP transforms the original
blind separation/dereverberation problem to a non-blind one.

We start the discussion with a single source and then consider
extensions to multiple sources.

### III-A MCLP for a Single Source

Consider a signal $`x`$ that is observed via different channels as

|     |                                               |     |      |
|-----|-----------------------------------------------|-----|------|
|     |
       ``` math
       y_{i}=x\ast h_{i},\text{ for }i=1,2,\ldots,N,
       ```                                            |     | (10) |

where $`h_{i}`$ denotes the impulse response of the $`i^{\text{th}}`$
channel. In the $`z`$-domain, this can be written as,

|  |  |  |  |
|----|----|----|----|
|  |
``` math
\begin{bmatrix}\mathcal{Y}_{1}(z)\\
\vdots\\
\mathcal{Y}_{N}(z)\end{bmatrix}=\begin{bmatrix}\mathcal{H}_{1}(z)\\
\vdots\\
\mathcal{H}_{N}(z)\end{bmatrix}\mathcal{X}(z).
``` |  | (11) |

We would like to find causal filters $`\mathcal{G}_{1}(z)`$, …,
$`\mathcal{G}_{N}(z)`$ such that

|  |  |  |  |
|----|----|----|----|
|  |
``` math
\begin{bmatrix}\mathcal{G}_{1}(z)&\ldots&\mathcal{G}_{N}(z)\end{bmatrix}\,\begin{bmatrix}\mathcal{H}_{1}(z)\\
\vdots\\
\mathcal{H}_{N}(z)\end{bmatrix}=1.
``` |  | (12) |

MINT \[[15](#bib.bib15)\] ensures that such filters exist provided that
$`\mathcal{H}_{i}`$’s are co-prime, i.e., they do not have a common
non-constant factor. Even though it is relieving to know that such
filters exist, finding the filters is still a challenging problem when
$`h_{i}`$’s are not known. MCLP addresses this challenge.

In MCLP, the idea is to estimate $`x`$ by subtracting from $`y_{r}`$
(for a chosen reference index $`r`$) delayed weighted combinations of
$`y_{i}`$’s. This is equivalent to using in
([12](#S3.E12 "In III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"))
a set of filters of the form

|  |  |  |  |  |
|----|----|----|----|----|
|  | $`\displaystyle\begin{split}\mathcal{G}_{r}(z)&=1-z^{-d}\,\mathcal{U}_{r}(z)\text{ for a reference index }r,\\
\mathcal{G}_{i}(z)&=-z^{-d}\,\mathcal{U}_{i}(z)\text{ for }i=1,\ldots,N,\,i\neq r,\end{split}`$ |  |  | (13) |

for a positive integer $`d`$ (determining the amount of delay) and
causal $`\mathcal{U}_{k}`$’s. In \[[11](#bib.bib11)\], the selection of
the filters is guided by the assumption that the original $`x`$ is
sparse. More specifically, suppose $`y_{i}(s)`$ denotes the 1D
time-series derived from a frequency band of the observations, that is
$`y_{i}(s)=Y_{i}(s,\omega)`$ for some $`\omega`$. The filters $`u_{i}`$
are found by solving the following minimization problem.

|  |  |  |  |
|----|----|----|----|
|  |
``` math
\min_{u_{1},\ldots,u_{N}}P\left(y_{r}(s)-\sum_{i=1}^{N}\sum_{l=0}^{L}y_{i}(s-d-l)\,u_{i}(l)\right),
``` |  | (14) |

where $`P(\cdot)`$ is a sparsity promoting function. We will have more
to say about $`P`$, but first we would like to stress a point about the
choice
([13](#S3.E13 "In III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")).
MINT ensures the existence of dereverberating filters but does not
guarantee that they can be of the form
([13](#S3.E13 "In III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")).
Below, we propose modified conditions which ensure the existence of such
$`G_{i}`$’s. Another issue is the role of the reference index $`r`$. It
turns out that repeating the linear prediction for different $`r`$
values leads to an interesting outcome, summarized in
Prop. [2](#Thmprop2 "Proposition 2. ‣ III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")
below. In order to derive
Prop. [2](#Thmprop2 "Proposition 2. ‣ III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"),
we need an auxiliary result
(Prop. [1](#Thmprop1 "Proposition 1. ‣ III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"))
and an assumption.

###### Proposition 1.

Suppose that the $`N-1`$ polynomials $`\mathcal{H}_{i}`$,
$`i\in\{1,\ldots,N\}\setminus\{r\}`$, are co-prime. Then, for $`d=1`$,
there exist a set of filters of the form
([13](#S3.E13 "In III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"))
such that

|  |  |  |  |
|----|----|----|----|
|  |
``` math
\begin{bmatrix}\mathcal{G}_{1}(z)&\ldots&\mathcal{G}_{N}(z)\end{bmatrix}\,\begin{bmatrix}\mathcal{H}_{1}(z)\\
\vdots\\
\mathcal{H}_{N}(z)\end{bmatrix}=c,
``` |  | (15) |

for a constant $`c`$. Further, for a set of causal filters of the form
([13](#S3.E13 "In III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")),
if
([15](#S3.E15 "In Proposition 1. ‣ III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"))
holds, then $`c=\mathcal{H}_{r}(\infty)`$.

###### Proof.

See
Appendix [A](#A1 "Appendix A Proof of Prop. ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation").
∎

Consider now the application of this result to the model
([9](#S2.E9 "In II-B An Approximate Model in the STFT Domain ‣ II Description of the Model ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")).
Assume that there is a single source, i.e., $`K=1`$. For a fixed
frequency $`\omega`$, let

|  |  |  |  |
|----|----|----|----|
|  |
``` math
h_{i}(s)=\tilde{H}_{i,1}(s,\omega)\,\exp(-j\omega\,\tau_{i,1}).
``` |  | (16) |

Under the hypotheses of
Prop. [1](#Thmprop1 "Proposition 1. ‣ III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"),
if, for $`d=1`$, MCLP can recover a set of filters satisfying
([15](#S3.E15 "In Proposition 1. ‣ III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")),
then
Prop. [1](#Thmprop1 "Proposition 1. ‣ III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")
implies that,

|     |                                                            |     |      |
|-----|------------------------------------------------------------|-----|------|
|     |
       ``` math
       \hat{y}_{r}(s)=h_{r}(0)\,\exp(-j\omega\,\tau_{r,1})\,x(s).
       ```                                                         |     | (17) |

Now, if further,

|  |  |  |  |
|----|----|----|----|
|  |
``` math
h_{i}(0)=h_{i^{\prime}}(0)\text{ for any distinct pair }(i,i^{\prime}),
``` |  | (18) |

then,

|     |                                                              |     |      |
|-----|--------------------------------------------------------------|-----|------|
|     |
       ``` math
       \hat{y}_{r}(s)=c^{\prime}\,\exp(-j\omega\,\tau_{r,1})\,x(s),
       ```                                                           |     | (19) |

for some constant $`c^{\prime}`$, independent of $`r`$ (actually,
$`c^{\prime}=h_{i}(0)`$, for any $`i`$). We remark that the difference
of
([17](#S3.E17 "In III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"))
and
([19](#S3.E19 "In III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"))
is that the rhs of
([19](#S3.E19 "In III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"))
depends only on the direction of arrival, whereas this is not the case
in
([17](#S3.E17 "In III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")).
In other words, MCLP eliminates reflections from different directions.

The assumption in
([18](#S3.E18 "In III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"))
states that, around the origin, distance compensated impulse responses
are similar. This assumption is key to the development in this paper and
we state it explicitly for later reference. We will verify this
assumption experimentally in
Section [V](#S5 "V Experiments ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation").

###### Assumption 1.

For a given source - microphone array pair, let
$`\tilde{H}_{i}(s,\omega)`$ denote the STFT representation of the
distance compensated impulse response for the $`i^{\text{th}}`$
microphone. Then,

|     |                                                           |     |      |
|-----|-----------------------------------------------------------|-----|------|
|     |
       ``` math
       \tilde{H}_{i}(0,\omega)=\tilde{H}_{i^{\prime}}(0,\omega),
       ```                                                        |     | (20) |

for $`i\neq i^{\prime}`$ and all $`\omega`$.

We state the foregoing observations as a proposition.

###### Proposition 2.

For a fixed frequency $`\omega`$, let
$`h_{i}(s)=\tilde{H}_{i,1}(s,\omega)\,\exp(-j\omega\,\tau_{i,1})`$.
Assume also that the collection of polynomials $`\mathcal{H}_{i}`$,
$`i\in\{1,\ldots,N\}\setminus\{r\}`$ are co-prime. Then, for $`d=1`$,
there exist a set of filters of the form
([13](#S3.E13 "In III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"))
such that

|     |                                                |     |      |
|-----|------------------------------------------------|-----|------|
|     |
       ``` math
       \sum_{i}g_{i}(n)\ast h_{i}(n)=c_{r}\,\delta(n)
       ```                                             |     | (21) |

for a constant $`c_{r}`$.

Further, for a set of filters of the form
([13](#S3.E13 "In III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")),
if
([21](#S3.E21 "In Proposition 2. ‣ III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"))
holds, and
Assumption [1](#Thmassm1 "Assumption 1. ‣ III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")
is in effect, then $`c_{r}=c^{\prime}\,\exp(-j\omega\tau_{r,1})`$ for a
constant $`c^{\prime}`$, independent of $`r`$. ∎

Next, we extend the discussion to the case of multiple sources.

### III-B MCLP for Multiple Sources

Consider an observation model with $`K`$ sources $`x_{1}`$,…$`x_{K}`$
and $`N>K`$ observations defined as,

|  |  |  |  |
|----|----|----|----|
|  |
``` math
y_{i}=\sum_{k=1}^{K}x_{k}\ast h_{i,k},\text{ for }i=1,2,\ldots,N.
``` |  | (22) |

In the $`z`$-domain, this can be written as,

|  |  |  |  |
|----|----|----|----|
|  |
``` math
\begin{bmatrix}\mathcal{Y}_{1}(z)\\
\vdots\\
\mathcal{Y}_{N}(z)\end{bmatrix}=\underbrace{\begin{bmatrix}\mathcal{H}_{1,1}(z)&\ldots&\mathcal{H}_{1,K}(z)\\
&\ddots&\\
\mathcal{H}_{N,1}(z)&\ldots&\mathcal{H}_{N,K}(z)\end{bmatrix}}_{\mathbf{H}}\begin{bmatrix}\mathcal{X}_{1}(z)\\
\ldots\\
\mathcal{X}_{K}(z)\end{bmatrix}
``` |  | (23) |

For this model, MINT also ensures that we can find filters
$`\mathcal{G}_{k,i}`$, for $`k=1,\ldots,K`$, $`i=1,\ldots,N`$ such that

|  |  |  |  |
|----|----|----|----|
|  |
``` math
\begin{bmatrix}\mathcal{G}_{1,1}(z)&\ldots&\mathcal{G}_{1,N}(z)\\
&\ddots&\\
\mathcal{G}_{K,1}(z)&\ldots&\mathcal{G}_{K,N}(z)\end{bmatrix}\,\begin{bmatrix}\mathcal{H}_{1,1}(z)&\ldots&\mathcal{H}_{1,K}(z)\\
&\ddots&\\
\mathcal{H}_{N,1}(z)&\ldots&\mathcal{H}_{N,K}(z)\end{bmatrix}=I,
``` |  | (24) |

provided that the (Smith) canonical form of $`\mathbf{H}`$ (see
Appendix [B](#A2 "Appendix B Canonical Form of a Polynomial Matrix ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")
or \[[8](#bib.bib8)\], Sec.VI.2, Defn.3) has a non-zero constant (i.e.,
no polynomial higher than zero order) diagonal. However, unlike the
single source case, we cannot use filters of the form
([13](#S3.E13 "In III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"))
to achieve
([24](#S3.E24 "In III-B MCLP for Multiple Sources ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")).
We have the following extension in this case.

###### Proposition 3.

For a fixed reference microphone index $`r\in\{1,\ldots,N\}`$, let
$`\bar{\mathbf{H}}_{r}`$ be the matrix obtained by removing the
$`r^{\text{th}}`$ row of $`\mathbf{H}`$. If the greatest common divisor
of all the $`K`$-minors of $`\bar{\mathbf{H}}_{r}`$ is a non-zero
constant, then, for $`d=1`$, there exist a set of filters of the form
([13](#S3.E13 "In III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"))
such that

|  |  |  |  |
|----|----|----|----|
|  |
``` math
\begin{bmatrix}\mathcal{G}_{1}(z)&\ldots&\mathcal{G}_{N}(z)\end{bmatrix}\,\begin{bmatrix}\mathcal{H}_{1,1}(z)&\ldots&\mathcal{H}_{1,K}(z)\\
&\ddots&\\
\mathcal{H}_{N,1}(z)&\ldots&\mathcal{H}_{N,K}(z)\end{bmatrix}=c^{(r)},
``` |  | (25) |

for a constant vector $`c^{(r)}`$ (i.e., containing no polynomial of
order greater than zero).

Further, for a set of filters of the form
([13](#S3.E13 "In III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")),
if
([25](#S3.E25 "In Proposition 3. ‣ III-B MCLP for Multiple Sources ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"))
holds, then the components of $`c`$ satisfy,

|  |  |  |  |
|----|----|----|----|
|  |
``` math
c^{(r)}_{k}=\mathcal{H}_{r,k}(\infty),\text{ for }k=1,2,\ldots,K.
``` |  | (26) |

###### Proof.

See
Appendix [C](#A3 "Appendix C Proof of Prop. ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation").
∎

###### Remark 1.

For an $`N\times K`$ matrix $`\mathbf{H}`$ with $`K\leq N`$, it can be
shown that, the greatest common divisor of all the $`K`$-minors of
$`\mathbf{H}`$ is a non-zero constant if and only if the Smith canonical
form of $`\mathbf{H}`$ has a non-zero constant diagonal. This
equivalence is shown in
Prop. [8](#Thmprop8 "Proposition 8. ‣ Appendix B Canonical Form of a Polynomial Matrix ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")
in
Appendix [B](#A2 "Appendix B Canonical Form of a Polynomial Matrix ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation").
Therefore, we can replace the hypothesis in
Prop. [3](#Thmprop3 "Proposition 3. ‣ III-B MCLP for Multiple Sources ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")
with an alternative, stated in terms of the Smith canonical form of
$`\bar{\mathbf{H}}_{r}`$.

When we apply this result to our observation model with $`K`$ sources,
we obtain the following proposition.

###### Proposition 4.

For a fixed frequency $`\omega`$, let

|  |  |  |  |
|----|----|----|----|
|  |
``` math
h_{i,k}(s)=\tilde{H}_{i,k}(s,\omega)\,\exp(-j\omega\,\tau_{i,k}),
``` |  | (27) |

for $`i\in\{1,\ldots,N\}`$, $`k\in\{1,\ldots,K\}`$. Also, let
$`\mathbf{H}`$ be defined as in
([23](#S3.E23 "In III-B MCLP for Multiple Sources ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")),
and $`\bar{\mathbf{H}}_{r}`$ be defined as in
Prop. [3](#Thmprop3 "Proposition 3. ‣ III-B MCLP for Multiple Sources ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation").
Finally, let $`y_{i}(s)=Y_{i}(s,\omega)`$.

If the greatest common divisor of all the $`K`$-minors of
$`\bar{\mathbf{H}}_{r}`$ is a non-zero constant, then, for $`d=1`$,
there exist a set of filters of the form
([13](#S3.E13 "In III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"))
such that

|     |                                                              |     |      |
|-----|--------------------------------------------------------------|-----|------|
|     |
       ``` math
       \sum_{i=1}^{N}g_{i}(s)\ast h_{i,k}(s)=c^{(r)}_{k}\,\delta(s)
       ```                                                           |     | (28) |

for some constants $`c^{(r)}_{k}`$.

Further, for a set of filters of the form
([13](#S3.E13 "In III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")),
if
([28](#S3.E28 "In Proposition 4. ‣ III-B MCLP for Multiple Sources ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"))
holds, and
Assumption [1](#Thmassm1 "Assumption 1. ‣ III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")
is in effect, then we have

|  |  |  |  |
|----|----|----|----|
|  |
``` math
\sum_{i=1}^{N}\,g_{i}(s)\ast y_{i}(s)=c^{\prime}\,\sum_{k=1}^{K}\,x_{k}(s)\,\exp(-j\,\omega\,\tau_{r,k}),
``` |  | (29) |

where $`c^{\prime}`$ is a constant independent of $`r`$.

###### Proof.

See
Appendix [D](#A4 "Appendix D Proof of Prop. ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation").
∎

This proposition suggests that, in a scenario with multiple sources,
MCLP, if successful, suppresses reflections and preserves the direct
signal even when there exist multiple sources from different directions.
Therefore, after the MCLP step, the recordings appear as if they are
recorded with the same microphone array, but in a non-reverberant
environment. This in turn allows to read off the directions of the
sources with a simple procedure like MUSIC \[[22](#bib.bib22)\]. Once
the source directions are determined, we can then achieve separation by
using *known* manifold vectors of the microphone array.

## IV Implementation Details

The discussion after
Prop. [4](#Thmprop4 "Proposition 4. ‣ III-B MCLP for Multiple Sources ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")
sets the stage for the proposed framework. The main building blocks of
the framework are shown in
Fig. [2](#S4.F2 "Fig. 2 ‣ IV Implementation Details ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation").
We first input the $`N`$ observations $`y_{1},\ldots,y_{N}`$, recorded
in a reverberant environment with unknown reverberation, to an MCLP
block. This block applies MCLP for every possible choice of the
reference microphone. The output of this block is $`N`$ dereverberated
signals $`\hat{y}_{1},\ldots,\hat{y}_{N}`$. These signals are input to a
DOA estimation algorithm (such as MUSIC \[[22](#bib.bib22)\]) and the
directions of the $`K`$ sources are determined. Then these estimated
directions are used to separate the sources in $`\hat{y}_{i}`$ using a
non-blind source separation method. The estimated sources are denoted as
$`\hat{x}_{i}`$ in
Fig. [2](#S4.F2 "Fig. 2 ‣ IV Implementation Details ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation").
In the following, we briefly present the algorithm we used for a
practical implementation of the components. However, we remark that
following MCLP, the problem turns into a non-reverberant source
separation problem (with known array geometry) and one can use any
method of choice for this task.

Fig. 2: The building blocks of the proposed framework. The input to the
system is $`y_{1}`$, …$`y_{N}`$, which are $`N`$ reverberant
observations of $`K`$ sources. Source directions are estimated under the
assumption of known array geometry. The estimated source directions are
used in a non-blind source-separation algorithm.

### IV-A Multi-Channel Linear Prediction

We use the formulation given in
Sec. [III-B](#S3.SS2 "III-B MCLP for Multiple Sources ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation").
We work in the STFT domain and process the frequency channels
separately. For a fixed value of $`\omega`$, we set
$`y_{i}(s)=Y_{i}(s,\omega)`$. Then for each value of
$`r\in\{1,\ldots,N\}`$, the dereverbed estimate $`\hat{y}_{r}`$ is
defined as,

|  |  |  |  |
|----|----|----|----|
|  |
``` math
\hat{y}_{r}(s)=y_{r}(s)-\sum_{i=1}^{N}\sum_{l=0}^{L-1}y_{i}(s-d-l)\,\hat{u}^{(r)}_{i}(l).
``` |  | (30) |

where $`\hat{u}^{(r)}_{i}`$ denote filters determined by solving

|  |  |  |  |
|----|----|----|----|
|  |
``` math
\left(\hat{u}^{(r)}_{1},\ldots,\hat{u}^{(r)}_{N}\right)\\
=\arg\min_{u_{1},\ldots,u_{N}}P\left(y_{r}(s)-\sum_{i=1}^{N}\sum_{l=0}^{L}y_{i}(s-d-l)\,u_{i}(l)\right).
``` |  | (31) |

We use a weighted $`\ell_{1}`$ norm as the sparsity promoting function
$`P(\cdot)`$. More precisely, for $`u\in\mathbb{C}^{n}`$, we set

|     |                                            |     |      |
|-----|--------------------------------------------|-----|------|
|     |
       ``` math
       P(u)=\|u\|_{w}=\sum_{i}^{n}w_{i}\,|u_{i}|,
       ```                                         |     | (32) |

where $`w\in\mathbb{R}_{+}^{n}`$ is a given weight vector. We update the
weight vector $`w`$ as the iterations progress, to improve the sparsity
promoting property of $`P`$.

We remark that for MCLP, a similar formulation has been discussed in
\[[11](#bib.bib11)\], along with an algorithm based on the alternating
direction method of multipliers (ADMM) algorithm \[[3](#bib.bib3)\]. We
employ the Douglas-Rachford (DR) algorithm \[[14](#bib.bib14),
[5](#bib.bib5)\] for solving this problem. To that end, we first define
the matrices $`X^{(i)}`$ for $`i\in\{1,\ldots,N\}`$ such that

|     |                                                          |     |      |
|-----|----------------------------------------------------------|-----|------|
|     |
       ``` math
       X^{(i)}_{s,l}=y_{i}(s-d-l),\text{ for }l=0,1,\ldots,L-1,
       ```                                                       |     | (33) |

and as $`s`$  ranges over the domain of the observation $`y_{r}`$.
Notice that $`X^{(i)}`$ is Toeplitz and has $`L`$ columns and as many
rows as the number of samples in $`y_{r}`$. By concatenating
$`X^{(i)}`$’s, we construct the data matrix $`X`$ as,

|     |                                                       |     |      |
|-----|-------------------------------------------------------|-----|------|
|     |
       ``` math
       X=\begin{bmatrix}X^{(1)}&\cdots&X^{(n)}\end{bmatrix}.
       ```                                                    |     | (34) |

Similarly, we construct the variable vector $`u`$ as

|     |                            |     |      |
|-----|----------------------------|-----|------|
|     |
       ``` math
       u=\begin{bmatrix}u^{(1)}\\
       \vdots\\
       u^{(N)}\end{bmatrix},
       ```                         |     | (35) |

where $`u^{(i)}`$ denotes the column vector with
$`u^{(i)}_{k}=u_{i}(k)`$. Also, let $`y^{(r)}`$ denote the column vector
with $`y^{(r)}_{k}=y_{r}(k)`$. Using these, we can rewrite the problem
([31](#S4.E31 "In IV-A Multi-Channel Linear Prediction ‣ IV Implementation Details ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"))
as,

|     |                                                     |     |      |
|-----|-----------------------------------------------------|-----|------|
|     |
       ``` math
       \min_{z,u}\|z\|_{w},\text{ subject to }z=y^{r}-X\,u
       ```                                                  |     | (36) |

We split variables and introduce $`z`$ in order to make the formulation
feasible for employing DR.

For this formulation, the Douglas-Rachford algorithm is of the form
outlined in
Algorithm [1](#alg1 "Algorithm 1 ‣ IV-A Multi-Channel Linear Prediction ‣ IV Implementation Details ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation").
We remark that in this algorithm, at each $`M`$ iterations, the weights
$`w`$ used to compute $`\|\cdot\|_{w}`$ are updated, so as to improve
the sparsity inducing property of the penalty function, following
\[[11](#bib.bib11)\]. We refer to \[[11](#bib.bib11)\] for more
intricate update strategies.

1: Input Data $`y^{(r)}`$, $`X`$, a constant for weight updates
$`\epsilon>0`$

2: Initialize $`w_{i}\leftarrow 1`$ for all $`i`$

3: repeat

4:   for m = 1 : M do $`\triangleright`$ Update $`w`$ every $`M`$
iterations

5:    $`\tilde{z}_{i}\leftarrow 2\,\soft(z,\,\alpha\,w)-z`$

6:
   $`\tilde{u}\leftarrow(I+X^{T}\,X)^{-1}\,\bigl(u+X^{T}\,(y^{r}-\tilde{z})\bigr)`$

7:
   $`\tilde{z}\leftarrow(1-\lambda)\,z+\lambda\,\bigl(2\,(y-X\,\tilde{u})-\tilde{z}\bigr)`$

8:    $`u\leftarrow(1-2\lambda)\,u+2\lambda\,\tilde{u}`$   

9:   $`\tilde{z}\leftarrow\soft(z,\,\alpha\,w)`$

10:   $`w_{i}\leftarrow\epsilon/(\epsilon+|z_{i}|)`$ for all $`i`$
$`\triangleright`$ update $`w`$

11: until convergence

12: return $`\hat{y}_{r}=\soft(z,\,\alpha\,w)`$

Algorithm 1 Multi-Channel Linear Prediction

### IV-B DOA Estimation

Because audio is a wideband signal, narrowband DOA estimation techniques
like MUSIC \[[22](#bib.bib22)\], or ESPRIT \[[21](#bib.bib21)\] are not
directly applicable. However, simple extensions of MUSIC to wideband
scenarios exist and we employ one of them. In a nutshell, the idea is to
estimate the DOA for each frequency separately and average the
estimates. This approach is referred to as the ‘incoherent signal
subspace method’ (ISSM) in the recent literature \[[32](#bib.bib32)\] –
see \[[30](#bib.bib30), [25](#bib.bib25)\] for early work. Despite its
simplicity, ISSM is noted to perform well in high SNR
\[[32](#bib.bib32)\].

Subspace methods for DOA estimation make use of two essential
components, namely the covariance matrix of the data and the manifold
vector, determined by the array geometry. For a fixed frequency
$`\omega`$, and time instance $`s`$ (in the STFT domain), we estimate
the correlation matrix $`R_{Y}(s,\omega)`$ as

|     |                                                                |     |      |
|-----|----------------------------------------------------------------|-----|------|
|     |
       ``` math
       R_{Y}(s,\omega)=(1-\eta)\,M(s,\omega)+\eta\,R_{Y}(s-1,\omega),
       ```                                                             |     | (37) |

where

|  |  |  |  |
|----|----|----|----|
|  |
``` math
M(s,\omega)=\begin{bmatrix}\hat{Y}_{1}(s,\omega)\\
\vdots\\
\hat{Y}_{N}(s,\omega)\end{bmatrix}\,\begin{bmatrix}\hat{Y}_{1}^{*}(s,\omega)&\cdots&\hat{Y}_{N}^{*}(s,\omega)\end{bmatrix}.
``` |  | (38) |

In this definition, $`\eta\in[0,1]`$ is a smoothing (or forgetting)
factor justified by the assumption that the sources move slowly.

Let us now define the manifold vector. Recall from
Sec. [II-A](#S2.SS1 "II-A A Time-Domain Observation Model ‣ II Description of the Model ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")
that $`p_{i}`$ denotes the position of the $`i^{\text{th}}`$ microphone
with respect to the center of the array. Also, let $`u_{\theta}`$ denote
the unit vector in the direction $`\theta`$. Finally, let $`c`$ denote
the speed of sound, $`f_{s}`$ denote the sampling frequency and

|  |  |  |  |
|----|----|----|----|
|  |
``` math
\tau_{i,\theta}=-\frac{\langle p_{i},u_{\theta}\rangle}{c}\,f_{s}
``` |  | (39) |

denote the relative delay (in samples) of the $`i^{\text{th}}`$
microphone for a source in the direction $`\theta`$. The manifold vector
is defined as

|  |  |  |  |
|----|----|----|----|
|  |
``` math
a_{\theta}(\omega)=\begin{bmatrix}\exp\bigl(-j\tau_{1,\theta}\,\omega\bigr)\\
\vdots\\
\exp\bigl(-j\tau_{N,\theta}\,\omega\bigr)\end{bmatrix}.
``` |  | (40) |

Assuming there are $`K`$ sources, let $`C_{K}(s,\omega)`$ denote the
$`(N-K)\times N`$ unitary matrix whose columns are the eigenvectors of
$`R_{Y}(s,\omega)`$ corresponding to the smallest $`N-K`$ eigenvalues.
The DOA function is defined as

|  |  |  |  |
|----|----|----|----|
|  |
``` math
D(s,\theta)=\left(\sum_{\omega}a_{\theta}^{*}(\omega)\,C_{K}(s,\omega)\,a_{\theta}(\omega)\right)^{-1}.
``` |  | (41) |

For each time instance $`s`$, the $`K`$ dominant peaks of
$`D(s,\theta)`$ with respect to $`\theta`$ are taken to be the
directions of the sources.

### IV-C Source Separation

For source separation, we use the geometric source separation (GSS)
method \[[18](#bib.bib18), [27](#bib.bib27)\] complemented with a simple
post-filter. We assume that the sources are stationary, so that the
estimated directions are constant with respect to time. Extension to
moving sources is straightforward, at least in principle, and will not
be further discussed.

#### IV-C1 Geometric Source Separation

Let $`\theta_{i}`$, for $`i=1,\ldots,K`$ denote the directions of the
sources determined by the method described in
Sec. [IV-B](#S4.SS2 "IV-B DOA Estimation ‣ IV Implementation Details ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation").
For each $`\omega`$, we define a matrix $`A(\omega)`$ as,

|  |  |  |  |
|----|----|----|----|
|  |
``` math
A(\omega)=\begin{bmatrix}a_{\theta_{1}}(\omega)&\cdots&a_{\theta_{K}}(\omega)\end{bmatrix}
``` |  | (42) |

We model the MCLP outputs as

|  |  |  |  |
|----|----|----|----|
|  |
``` math
\underbrace{\begin{bmatrix}\hat{Y}_{1}(s,\omega)\\
\vdots\\
\hat{Y}_{N}(s,\omega)\end{bmatrix}}_{\hat{Y}(s,\omega)}=A(\omega)\,\underbrace{\begin{bmatrix}X_{1}(s,\omega)\\
\vdots\\
X_{K}(s,\omega)\end{bmatrix}}_{X(s,\omega)}.
``` |  | (43) |

This model is not exact because despite the significant dereverberation
by MCLP, $`\hat{Y}`$ still contains reflections other than the direct
signal.

For this model, GSS proposes to estimate $`X(s,\omega)`$ as
$`\hat{W}(\omega)\,\hat{Y}(s,\omega)`$, where the $`K\times N`$ weight
matrix $`\hat{W}(\omega)`$ is determined by solving a minimization
problem. To describe the problem, let us denote

|     |                                     |     |      |
|-----|-------------------------------------|-----|------|
|     |
       ``` math
       S(s,\omega)=W(\omega)\,Y(s,\omega),
       ```                                  |     | (44) |

for a given weight matrix $`W`$. Also, let $`R_{S}(\omega)`$ denote the
empirical autocorrelation matrix of $`S(s,\omega)`$, obtained by
averaging $`S(s,\omega)\,S^{*}(s,\omega)`$ over the time variable $`s`$.
Given this notation, $`\hat{W}(\omega)`$ is defined as the solution of

|  |  |  |  |
|----|----|----|----|
|  |
``` math
\min_{W(\omega)}\left\{C\bigl(W(\omega)\bigr)=\frac{\gamma}{2}\,\bigl\|R_{S}(\omega)-\diag\bigl(R_{S}(\omega)\bigr)\bigr\|_{F}^{2}\right.\\
\left.+\frac{1}{2}\,\bigl\|W(\omega)\,A(\omega)-I\bigr\|_{F}^{2}\right\},
``` |  | (45) |

where $`\|\cdot\|_{F}`$ denotes the Frobenius norm.

Thanks to the differentiability of both terms in the cost function,
gradient descent can be applied for obtaining a stationary point. For a
fixed $`\omega`$, we remark that the gradient \[[4](#bib.bib4)\] of the
complex valued $`C(W)`$ is given as \[[18](#bib.bib18)\],

|  |  |  |  |
|----|----|----|----|
|  |
``` math
\frac{\partial C(W)}{\partial W^{*}}=\gamma\,\bigl(R_{S}-\diag(R_{S})\bigr)\,W\,R_{\hat{Y}}+(W\,A-I)\,A^{*},
``` |  | (46) |

where $`R_{\hat{Y}}`$ denotes the empirical autocorrelation matrix of
$`\hat{Y}`$, obtained by averaging
$`\hat{Y}^{*}(s,\omega)\,\hat{Y}(s,\omega)`$ over the time variable
$`s`$.

#### IV-C2 Post-Filter

The GSS algorithm aims to achieve source separation by considering the
best linear combination of the inputs. In practice, separation
performance can be improved by post-filtering. Specifically, we view the
separated signals as contaminated with noise and eliminate this noise
using a soft-threshold, where the time-frequency varying threshold value
depends on an estimate of the remaining noise variance for each
time-frequency sample.

More specifically, if $`\hat{S}_{1}`$, …, $`\hat{S}_{K}`$ denotes the
$`K`$ source estimates obtained by GSS, for each value of the triplet
$`(i,s,\omega)`$, we regard $`\hat{S}_{i}(s,\omega)`$ as an unbiased
estimate of $`X_{i}(s,\omega)`$ with variance
$`\sigma_{i}^{2}(s,\omega)`$. The post-filtered estimate is formed as,

|  |  |  |  |
|----|----|----|----|
|  |
``` math
\hat{X}_{i}(s,\omega)=\soft\Bigl(\hat{S}_{i}(s,\omega),\alpha\,\sigma_{i}(s,\omega)\Bigr),
``` |  | (47) |

where $`\alpha`$ is a parameter.

In order to realize such a post-filter, we need to estimate
$`\sigma_{i}^{2}(s,\omega)`$. We estimate $`\sigma_{i}^{2}(s,\omega)`$
as,

|  |  |  |  |
|----|----|----|----|
|  |
``` math
\frac{1}{N-1}\,\sum_{n=1}^{N}\,|W_{i,n}(s,\omega)|\\
\times\left|\Bigl(\hat{Y}_{n}(s,\omega)-a_{\theta_{i},n}(\omega)\,\hat{S}_{i}(s,\omega)\Bigr)\right|^{2},
``` |  | (48) |

where $`a_{\theta_{i},n}(\omega)`$ denotes the $`n^{\text{th}}`$ entry
of $`a_{\theta_{i}}(\omega)`$. While this expression may make intuitive
sense, further justification is provided in
Appendix [E](#A5 "Appendix E Motivation for the Post-Filter in Sec. ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation").
We also remark that this is a generalized version of the post-filter
employed in \[[1](#bib.bib1)\].

## V Experiments

Our primary claim in this paper is that an MCLP step suppresses
reflections other than the direct source, under a certain assumption
(Assumption [1](#Thmassm1 "Assumption 1. ‣ III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")).
The experiments in this section are geared towards demonstrating the
claims and verifying
Assumption [1](#Thmassm1 "Assumption 1. ‣ III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation").
There are three experiments. In the first, we verify
Assumption [1](#Thmassm1 "Assumption 1. ‣ III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")
using real impulse responses obtained in a classroom. In the second
experiment, we demonstrate that an MCLP preprocessing step eliminates
reflections other than the direct source, by showing how the DOA
function changes after the MCLP step. Finally, the third experiment
validates our claim about the effect of employing an MCLP preprocessing
step in a source separation problem.

All of the experiments are performed on real data. In order to obtain
the data, we used a circular microphone array with eight
omni-directional microphones. The configuration and the definition of
DOA, namely $`\theta`$, are as shown in
Fig. [3](#S5.F3 "Fig. 3 ‣ V Experiments ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation").
The microphones are distributed uniformly on a circle of radius 5 cm.

In the experiments, we use an STFT with a smooth window of length 64 ms,
and hop-size is 16 ms. The window is selected such that the STFT is
self-inverting (i.e., a Parseval frame). The sampling frequency for the
audio signals is 16 KHz. For the MCLP block, the parameters (see
Algorithm [1](#alg1 "Algorithm 1 ‣ IV-A Multi-Channel Linear Prediction ‣ IV Implementation Details ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"))
are chosen as $`d=2`$, $`L=30`$, $`M=50`$, $`\alpha=0.05`$,
$`\lambda=0.5`$. We run the algorithm for 2000 iterations.

Some of the audio signals used in the experiments, as well as those from
experiments not contained in the paper can be listened to at
“http://web.itu.edu.tr/ibayram/MCLP/”.

![Refer to caption](1702.07713v1/fig3.png)

Fig. 3: We use an array of eight microphones distributed uniformly on a
circle. $`\theta`$ denotes the angle of arrival. In particular,
$`\theta=0`$ radians if the source is in the direction of $`m_{1}`$,
$`\theta=\pi/2`$ radians if the source is in the direction of $`m_{7}`$,
etc.

###### Experiment 1 (Verification of Assumption [1](#Thmassm1 "Assumption 1. ‣ III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")).

In order to verify
Assumption [1](#Thmassm1 "Assumption 1. ‣ III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"),
we used estimated impulse responses from different DOAs. The impulse
responses are estimated using the method described in
\[[6](#bib.bib6)\], using loudspeakers placed at known DOAs in the far
field. Knowing the DOAs, we computed the distance compensated impulse
responses and obtained filters $`\tilde{H}_{i}(s,\omega)`$ that
approximate their action in the STFT domain. For each $`s`$ and
$`\omega`$ value, we computed the variance of
$`\tilde{H}_{i}(s,\omega)`$’s with respect to $`i`$ (recall $`i`$ takes
eight different values), which we denote as $`v(s,\omega)`$. We
normalize $`v(s,\omega)`$ by the total power at the location $`s`$,
$`\omega`$, defined by $`p(s,\omega)=\sum_{i}|H_{i}(s,\omega)|^{2}`$.
Thus for each $`s`$ and $`\omega`$ value, we obtained a number
$`\hat{v}(s,\omega)=v(s,\omega)/p(s,\omega)`$. We remark that
Assumption [1](#Thmassm1 "Assumption 1. ‣ III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")
is valid if and only if $`\hat{v}(0,\omega)=0`$ for all $`\omega`$. In
Fig. [4](#S5.F4 "Fig. 4 ‣ Experiment 1 (Verification of Assumption ). ‣ V Experiments ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"),
we show (in decibels) the fields $`\hat{v}(s,\omega)`$ for different
DOAs, namely $`0`$, $`\pi/4`$ and $`\pi/2`$ radians. Observe that for
all DOAs and almost all $`\omega`$ shown, $`\hat{v}(0,\omega)`$ (the
first column in each image) is significantly lower than
$`\hat{v}(s,\omega)`$ for $`s\neq 0`$ (the remaining columns).
Therefore,
Assumption [1](#Thmassm1 "Assumption 1. ‣ III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")
is seen to be valid for practical purposes.

![Refer to caption](1702.07713v1/fig4.png)

Fig. 4:
Experiment [1](#Thmexperiment1 "Experiment 1 (Verification of Assumption ). ‣ V Experiments ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")
: ‘Correlation figures’ (see the text for a description), in dB, for the
circular array and different directions of arrival. (a) DOA = 0 rad, (b)
DOA = $`\pi/4`$ rad, (c) DOA = $`\pi/2`$ rad.

###### Experiment 2 (Direction of Arrival).

In this experiment, we demonstrate the effect of an MCLP front-end on
estimating the DOA.

The source signal is produced with a loudspeaker located at
$`\theta=\pi/2`$ radians in a reverberant environment. The spectrogram
of the signal observed at the first microphone is shown in
Fig. [5](#S5.F5 "Fig. 5 ‣ Experiment 2 (Direction of Arrival). ‣ V Experiments ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")a.
Notice that the harmonics are seriously smeared. For this signal, MCLP
significantly removes the effects of reverberation – see
Fig. [5](#S5.F5 "Fig. 5 ‣ Experiment 2 (Direction of Arrival). ‣ V Experiments ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")b.

![Refer to caption](1702.07713v1/fig5.png)

Fig. 5:
Experiment [2](#Thmexperiment2 "Experiment 2 (Direction of Arrival). ‣ V Experiments ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")
: Spectrograms of the signals used in the experiment. (a) The
observation at the first microphone. (b) MCLP output when the first
microphone is used as the reference.

In order to visualize the effects of MCLP on DOA estimation, we show the
DOA function $`D(k,\theta)`$ (defined in
([41](#S4.E41 "In IV-B DOA Estimation ‣ IV Implementation Details ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")))
averaged over the time variable $`k`$ in
Fig. [6](#S5.F6 "Fig. 6 ‣ Experiment 2 (Direction of Arrival). ‣ V Experiments ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"),
for both the reverberant observations and MCLP outputs. We remark that
averaging is meaningful here, since the source is stationary. For
comparison, we also normalize the averaged DOA functions so that their
peak is unity. The correct position of the source is also indicated with
a dotted line. We see that the DOA function is clearly improved by the
application of MCLP. The variance is clearly reduced and the function is
closer to an impulse located at the correct position.

![Refer to caption](1702.07713v1/fig6.png)

Fig. 6:
Experiment [2](#Thmexperiment2 "Experiment 2 (Direction of Arrival). ‣ V Experiments ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")
: The DOA functions $`D(k,\theta)`$ (see
([41](#S4.E41 "In IV-B DOA Estimation ‣ IV Implementation Details ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")))
averaged over time (i.e., $`s`$) for the reverberant observations and
MCLP outputs. We also apply a normalization to each DOA function so that
the peak is equal to unity.

###### Experiment 3 (Source Separation).

In this experiment, we demonstrate the improvement gained by employing
MCLP as a pre-processing step in a blind separation setup. We use the
same microphone array as in
Exp. [2](#Thmexperiment2 "Experiment 2 (Direction of Arrival). ‣ V Experiments ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation").
We use the source from
Exp. [2](#Thmexperiment2 "Experiment 2 (Direction of Arrival). ‣ V Experiments ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")
as the first source. The second source is produced by a loudspeaker
placed at $`\theta=1.75\,\pi`$ radians (so that the sources are
separated by $`0.75\,\pi`$ radians). The spectrogram of the observation
at the first microphone is shown in
Fig. [7](#S5.F7 "Fig. 7 ‣ Experiment 3 (Source Separation). ‣ V Experiments ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")a.
The output of MCLP when the first microphone is taken as the reference
is shown in
Fig. [7](#S5.F7 "Fig. 7 ‣ Experiment 3 (Source Separation). ‣ V Experiments ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")b.

![Refer to caption](1702.07713v1/fig7.png)

Fig. 7:
Experiment [3](#Thmexperiment3 "Experiment 3 (Source Separation). ‣ V Experiments ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")
: (a) Spectrogram of the two mixture signal observed at the first
microphone. (b) MCLP applied to the observations, when the first
microphone is taken as the reference.

The average (over time) DOA functions obtained from the original
recordings and MCLP outputs are shown in
Fig. [8](#S5.F8 "Fig. 8 ‣ Experiment 3 (Source Separation). ‣ V Experiments ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation").
We observe that MCLP has considerably sharpened the functions. Also,
without MCLP, the peaks of the DOA functions are offset by some amount
from the actual directions, which would lead to a biased estimate of the
true DOA.

![Refer to caption](1702.07713v1/fig8.png)

Fig. 8:
Experiment [3](#Thmexperiment3 "Experiment 3 (Source Separation). ‣ V Experiments ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")
: The DOA functions $`D(k,\theta)`$ (see
([41](#S4.E41 "In IV-B DOA Estimation ‣ IV Implementation Details ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")))
averaged over time (i.e., $`k`$) for the reverberant observations and
MCLP outputs. We also apply a normalization to each DOA function so that
the peak is equal to unity.

The source separation results are shown in
Fig. [9](#S5.F9 "Fig. 9 ‣ Experiment 3 (Source Separation). ‣ V Experiments ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation").
Perceptually, without MCLP, especially for the second source, we can
clearly hear the first source, despite the post-filter. Further,
reverberation is not eliminated. These artifacts are eliminated using
MCLP.

![Refer to caption](1702.07713v1/fig9.png)

Fig. 9:
Experiment [3](#Thmexperiment3 "Experiment 3 (Source Separation). ‣ V Experiments ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")
: Spectrograms of the sources separated with and without MCLP. (a,b)
Source-1,2 obtained with GSS and a post-filter, without MCLP. (c,d)
Source-1,2 obtained when MCLP is applied first.

In order to quantify the results, we also report PESQ and SIR scores
\[[29](#bib.bib29)\]. PESQ gives an objective perceptual distance
between an input speech signal and a reference. In order to obtain
meaningful distances, we use the multiple channel recordings of source-1
and source-2, prior to mixing, to extract the reference signals. We take
the $`i^{\text{th}}`$ microphone as reference and apply MCLP to the
multiple microphone recordings to obtain the references for each source.
We then compute the PESQ scores for the methods described in this paper.
We finally average over $`i`$ to obtain a reliable PESQ score. The
resulting scores are tabulated in
Table [I](#S5.T1 "TABLE I ‣ Experiment 3 (Source Separation). ‣ V Experiments ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation").
We remark that the inclusion of an MCLP step (denoted as LP in the
table) improves the PESQ scores noticeably.

The SIR scores for separation with different combinations of the
discussed methods are also provided in
Table [I](#S5.T1 "TABLE I ‣ Experiment 3 (Source Separation). ‣ V Experiments ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation").
We observe that including an MCLP preprocessing step also improves
significantly the separation quality measured by SIR.

|      |
|------|
| PESQ |

| Reference | Obs. | LP   | G    | G+P  | LP+G | LP+G+P |
|-----------|------|------|------|------|------|--------|
| Source-1  | 1.07 | 1.04 | 1.05 | 1.08 | 1.10 | 1.20   |
| Source-2  | 1.04 | 1.08 | 1.05 | 1.14 | 1.22 | 1.49   |

|     |
|-----|
| SIR |

|     |      |      |        |
|-----|------|------|--------|
| G   | G+P  | LP+G | LP+G+P |
| 5.0 | 13.0 | 7.8  | 19.5   |

TABLE I: PESQ and SIR scores for
Exp. [3](#Thmexperiment3 "Experiment 3 (Source Separation). ‣ V Experiments ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation").
(LP : Multichannel Linear Prediction, G : Geometric Source Separation, P
: Post filter)

## VI Conclusion

In this paper, we advocated the use of MCLP as a pre-processing step for
blind reverberant source separation. We provided theoretical
justification for such a scheme and demonstrated numerically that it can
significantly improve the performance of readily available separation
algorithms.

The current algorithm for MCLP is computationally intensive. However,
for a practical scenario with slowly moving sources, if we have
approximate MCLP weights at a certain time, we think it would be
possible to update the weights in a computationally efficient manner by
warm-starting the optimization procedure. Therefore, the proposed scheme
has potential for real-time applications as well.

Another issue involves the correlation between the weights obtained for
different reference microphones. In the current formulation, MCLP is
applied independently for each choice of the reference microphone.
However, we would expect some correlation between these weights,
especially if the microphones are close with respect to the room
dimensions, which is usually the case for a microphone array. We plan to
explore the implications of such correlation in future work.

## Appendix A Proof of Prop. [1](#Thmprop1 "Proposition 1. ‣ III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")

Without loss of generality, suppose $`r=1`$ in the following.

Since $`\mathcal{H}_{2}`$, …, $`\mathcal{H}_{N}`$ are co-prime, we can
find causal polynomials $`\tilde{\mathcal{U}}_{2}`$, …,
$`\tilde{\mathcal{U}}_{N}`$ such that

|  |  |  |  |
|----|----|----|----|
|  |
``` math
\sum_{k=2}^{N}\mathcal{\tilde{U}}_{k}(z)\,\mathcal{H}_{k}(z)=1.
``` |  | (49) |

Now set, $`\mathcal{U}_{1}(z)=1`$ and

|  |  |  |  |
|----|----|----|----|
|  |
``` math
\mathcal{U}_{k}(z)=\tilde{\mathcal{U}}_{k}(z)\,\frac{\mathcal{H}_{1}(z)-\mathcal{H}_{1}(\infty)}{z^{-1}}.
``` |  | (50) |

Then for $`\mathcal{G}`$ as in
([13](#S3.E13 "In III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")),
and $`d=1`$, we have

|  |  |  |  |  |
|----|----|----|----|----|
|  | $`\displaystyle\sum_{k=1}^{N}\mathcal{G}_{k}(z)\,\mathcal{H}_{k}(z)`$ | $`\displaystyle=\mathcal{H}_{1}(z)-\bigl(\mathcal{H}_{1}(z)-\mathcal{H}_{1}(\infty)\bigr)`$ |  | (51) |
|  |  | $`\displaystyle\quad\quad\quad\,\times\sum_{k=2}^{N}\,\mathcal{\tilde{U}}_{k}(z)\,\mathcal{H}_{k}(z)`$ |  | (52) |
|  |  | $`\displaystyle=\mathcal{H}_{1}(z)-\bigl(\mathcal{H}_{1}(z)-\mathcal{H}_{1}(\infty)\bigr)`$ |  | (53) |
|  |  | $`\displaystyle=\mathcal{H}_{1}(\infty).`$ |  | (54) |

For the converse, observe that for causal $`\mathcal{U}_{k}`$ and
$`\mathcal{H}_{k}`$, the polynomial

|  |  |  |  |
|----|----|----|----|
|  |
``` math
\mathcal{C}(z)=\sum_{k=1}^{N}z^{-d}\,\mathcal{U}_{k}(z)\,\mathcal{H}_{k}(z)
``` |  | (55) |

does not have any constant terms, i.e., $`C(\infty)=0`$. Observe also
that $`\mathcal{G}_{1}(\infty)=1`$. Thus if
([15](#S3.E15 "In Proposition 1. ‣ III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"))
holds, then

|  |  |  |  |  |
|----|----|----|----|----|
|  | $`\displaystyle\left.\sum_{k=1}^{N}\mathcal{G}_{k}(z)\,\mathcal{H}_{k}(z)\right|_{z=\infty}`$ | $`\displaystyle=\mathcal{G}_{1}(\infty)\,\mathcal{H}_{1}(\infty)-C(\infty)`$ |  | (56) |
|  |  | $`\displaystyle=\mathcal{H}_{1}(\infty).`$ |  | (57) |

## Appendix B Canonical Form of a Polynomial Matrix

In this appendix, we briefly present, for the sake of completeness, the
(Smith) canonical form of a polynomial matrix, along with some of its
properties. For a more detailed exposition, we refer to the fine
treatment in Sec. VI.1–3 of Gantmacher’s book \[[8](#bib.bib8)\].

We refer to a quantity or matrix as non-zero constant if it is a
polynomial of order zero.

Let us start by introducing three *elementary row operations* on a
polynomial matrix $`\mathbf{A}(z)`$ :

1.  1.
    Multiplication of a row with a non-zero constant.
2.  2.
    Addition, to the $`j^{\text{th}}`$ row, the product of the
    $`i^{\text{th}}`$ row and a polynomial $`p(z)`$.
3.  3.
    Interchange of two rows.

These operations can be performed by left multiplication with a square
matrix whose determinant is a non-zero constant. We refer to matrices
that can be used to perform elementary operations as *elementary
matrices* in this appendix. We also remark that similar operations can
be defined on the columns instead of rows. Elementary column operations
can be performed by right-multiplication with elementary matrices.

We say that two polynomial matrices $`\mathbf{A}`$ and
$`\mathbf{A}^{\prime}`$ are equivalent if $`\mathbf{A}`$ can be
transformed to $`\mathbf{A}^{\prime}`$ by elementary row/column
operations.

Let us now introduce the (Smith) canonical form.

###### Definition 1.

An $`N\times K`$ (with $`K\leq N`$) diagonal polynomial matrix
$`\mathbf{A}(z)`$ of rank $`r`$ with diagonal $`a_{i}(z)`$ for
$`i\in\{1,\ldots,K\}`$ is said to be in (Smith) canonical form if

1.  1.
    $`a_{i}(z)\neq 0`$ if $`i\leq r`$, $`a_{i}(z)=0`$ if $`i>r`$,
2.  2.
    $`a_{i}(0)=1`$ for $`i\leq r`$,
3.  3.
    $`a_{i}(z)`$ divides $`a_{i+1}(z)`$ for $`i<r`$.

A fundamental result is that we can transform any polynomial matrix to
one in canonical form via elementary row/column operations.

###### Proposition 5.

Each $`N\times K`$ polynomial matrix is equivalent to a unique matrix in
canonical form.

In general, the canonical form is reached by performing both row and
column operations. However, we are interested in row operations only,
for the development in this paper. We have the following result.

###### Proposition 6.

Suppose an $`N\times K`$ matrix $`A`$ has rank $`K`$ and the diagonal of
its canonical form is constant. Then, $`A`$ can be reduced to its
canonical form via row operations.

This result is a corollary of the discussion in \[[8](#bib.bib8)\] but
is not stated explicitly. We provide a proof for the sake of
completeness. We need a few intermediate results for the proof. All of
the non-obvious claims stated before the proof of
Prop. [6](#Thmprop6 "Proposition 6. ‣ Appendix B Canonical Form of a Polynomial Matrix ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")
can be found in \[[8](#bib.bib8)\].

Given an $`N\times K`$ polynomial matrix $`A(z)`$, let $`D_{r}(z)`$
denote the greatest common divisor of all minors of $`A`$ of order
$`r`$, normalized such that the leading coefficient is unity. Notice
that $`D_{r}`$ is divisible by $`D_{r-1}`$. Therefore, for $`r>1`$, the
ratios $`i_{r}=D_{r}/D_{r-1}`$ are polynomials. We also set
$`i_{1}=D_{1}`$. It can be shown that $`D_{r}`$’s are invariant under
elementary row/column operations. This property transfers to $`i_{r}`$
as a consequence of its definition.

Suppose now that the canonical form of $`A(z)`$ consists of a diagonal
matrix with elements $`a_{r}(z)`$ as in
Defn. [1](#Thmdefn1 "Definition 1. ‣ Appendix B Canonical Form of a Polynomial Matrix ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation").
It turns out that $`a_{r}=i_{r}`$, giving us an alternative definition
of $`a_{r}`$.

We need a final result before proving
Prop. [6](#Thmprop6 "Proposition 6. ‣ Appendix B Canonical Form of a Polynomial Matrix ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"),
which can be shown by a simple elimination argument.

###### Proposition 7.

Using elementary row operations, any polynomial matrix $`B(z)`$ can be
reduced to an upper triangular matrix $`U(z)`$ such that, at each column
of $`U(z)`$, if the polynomial on the diagonal is non-zero, then the
degree of any off-diagonal polynomial is strictly smaller than the
degree of the polynomial on the diagonal.

We are now ready for the proof of
Prop. [6](#Thmprop6 "Proposition 6. ‣ Appendix B Canonical Form of a Polynomial Matrix ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation").

###### Proof of Prop. [6](#Thmprop6 "Proposition 6. ‣ Appendix B Canonical Form of a Polynomial Matrix ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation").

Suppose the canonical form of $`A`$ is $`B`$. Observe that the only
non-zero minor of $`B`$ of order $`K`$ is given by the product of its
diagonals, which is unity. Therefore, the invariant polynomial $`D_{K}`$
of $`A`$ is a constant.

Suppose we reduce $`A`$ to an upper triangular form $`U`$ as in
Prop. [7](#Thmprop7 "Proposition 7. ‣ Appendix B Canonical Form of a Polynomial Matrix ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")
using elementary row operations only. Observe that the only non-zero
minor of $`U`$ of order $`K`$ is given by the product of its diagonals.
Let us denote this non-zero minor by $`U_{K}(z)`$. But since this is the
only non-zero minor of order $`K`$, and elementary matrices have
constant determinants, it follows that we must have
$`U_{K}(z)=c\,D_{K}`$ for some constant $`c`$. Thus, $`U_{K}(z)`$ is a
constant. Therefore, $`U`$ is a constant diagonal matrix with a non-zero
diagonal. Each constant on the diagonal can be made unity by an
elementary row operation, yielding the canonical form $`B`$. ∎

In a final proposition, we show the equivalence stated in
Remark [1](#Thmremark1 "Remark 1. ‣ III-B MCLP for Multiple Sources ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation").

###### Proposition 8.

Suppose $`A`$  is an $`N\times K`$  polynomial matrix with $`K\leq N`$.
The greatest common divisor of all the $`K`$-minors of $`A`$ is a
non-zero constant if and only if the Smith canonical form of $`A`$ has a
non-zero constant diagonal.

###### Proof.

As in the discussion above, let $`D_{r}(z)`$ denote the greatest common
divisor of all minors of $`A`$ of order $`r`$, where the leading
coefficient is unity, $`i_{r}=D_{r}/D_{r-1}`$ for $`r>1`$, with
$`i_{1}=D_{1}`$. Recall that $`i_{r}`$’s form the diagonal in the Smith
canonical form of $`A(z)`$.

Suppose now that $`D_{K}(z)=1`$. This implies that $`i_{r}=1`$ for all
$`r\leq K`$. Therefore the diagonal of the canonical form of $`A`$  is
the identity matrix.

For the converse, suppose the canonical form of $`A`$ has a constant
diagonal. This means that $`i_{r}(z)`$’s are constant for $`r\leq K`$.
But this implies that $`D_{r}(z)=1`$ for $`r\leq K`$. ∎

## Appendix C Proof of Prop. [3](#Thmprop3 "Proposition 3. ‣ III-B MCLP for Multiple Sources ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")

Without loss of generality, suppose $`r=1`$ in the following.

By
Remark [1](#Thmremark1 "Remark 1. ‣ III-B MCLP for Multiple Sources ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"),
which is proved in
Prop. [8](#Thmprop8 "Proposition 8. ‣ Appendix B Canonical Form of a Polynomial Matrix ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"),
we first note that the gcd of the $`K`$-minors of
$`\bar{\mathbf{H}}_{1}`$ is a non-zero constant if and only if the
canonical form of $`\bar{\mathbf{H}}_{1}`$ has a non-zero constant
diagonal. Thus, by
Prop. [6](#Thmprop6 "Proposition 6. ‣ Appendix B Canonical Form of a Polynomial Matrix ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"),
we can find a $`K\times(N-1)`$ polynomial matrix $`\mathbf{V}`$ such
that

|     |                                     |     |      |
|-----|-------------------------------------|-----|------|
|     |
       ``` math
       \mathbf{V}\,\bar{\mathbf{H}}_{1}=I.
       ```                                  |     | (58) |

For $`k=1,\ldots,K`$, let us define the polynomials

|  |  |  |  |
|----|----|----|----|
|  |
``` math
\mathcal{P}_{k}=\frac{\mathcal{H}_{1,k}(z)-\mathcal{H}_{1,k}(\infty)}{z^{-1}}
``` |  | (59) |

and set

|  |  |  |  |
|----|----|----|----|
|  |
``` math
\begin{bmatrix}\mathcal{U}_{2}&\cdots&\mathcal{U}_{N}\end{bmatrix}=\begin{bmatrix}\mathcal{P}_{1}&\cdots&\mathcal{P}_{K}\end{bmatrix}\,\mathbf{V}.
``` |  | (60) |

Also, set $`\mathcal{U}_{1}(z)=0`$. Then for $`\mathcal{G}`$ defined as
in
([13](#S3.E13 "In III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")),
we have

|  |  |  |  |
|----|----|----|----|
|  |
``` math
\begin{split}\begin{bmatrix}\mathcal{G}_{1}&\cdots&\mathcal{G}_{N}\end{bmatrix}\,\mathbf{H}=\begin{bmatrix}\mathcal{H}_{1,1}(z)&\cdots&\mathcal{H}_{1,K}(z)\end{bmatrix}\\
\quad-z^{-1}\,\begin{bmatrix}\mathcal{P}_{1}(z)&\cdots&\mathcal{P}_{K}(z)\end{bmatrix}\\
=\begin{bmatrix}\mathcal{H}_{1,1}(\infty)&\cdots&\mathcal{H}_{1,K}(\infty)\end{bmatrix}\end{split}
``` |  | (61) |

For the converse, observe that, for causal $`\mathcal{U}_{k}`$ and
$`\mathcal{H}_{i,k}`$, the polynomial

|  |  |  |  |
|----|----|----|----|
|  |
``` math
\mathcal{C}_{k}(z)=\sum_{i=1}^{N}z^{-d}\,\mathcal{U}_{i}(z)\,\mathcal{H}_{i,k}(z)
``` |  | (62) |

does not have any constant terms, regardless of $`k`$, i.e.,
$`C_{k}(\infty)=0`$ for all $`k`$. Observe also that
$`\mathcal{G}_{1}(\infty)=1`$. Thus if
([25](#S3.E25 "In Proposition 3. ‣ III-B MCLP for Multiple Sources ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"))
holds, then,

|  |  |  |  |  |
|----|----|----|----|----|
|  | $`\displaystyle\left.\sum_{i=1}^{N}\mathcal{G}_{i}(z)\,\mathcal{H}_{i,k}(z)\right|_{z=\infty}`$ | $`\displaystyle=\mathcal{G}_{1}(\infty)\,\mathcal{H}_{1,k}(\infty)-C_{k}(\infty)`$ |  | (63) |
|  |  | $`\displaystyle=\mathcal{H}_{1,k}(\infty).`$ |  | (64) |

## Appendix D Proof of Prop. [4](#Thmprop4 "Proposition 4. ‣ III-B MCLP for Multiple Sources ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")

We remark that
([28](#S3.E28 "In Proposition 4. ‣ III-B MCLP for Multiple Sources ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"))
follows by expressing
([25](#S3.E25 "In Proposition 3. ‣ III-B MCLP for Multiple Sources ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"))
in the time-domain.

Let us consider
([29](#S3.E29 "In Proposition 4. ‣ III-B MCLP for Multiple Sources ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")).
If
([25](#S3.E25 "In Proposition 3. ‣ III-B MCLP for Multiple Sources ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"))
holds, and $`\mathcal{G}_{i}`$’s satisfy
([13](#S3.E13 "In III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")),
then by
([26](#S3.E26 "In Proposition 3. ‣ III-B MCLP for Multiple Sources ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")),
we have

|  |  |  |  |
|----|----|----|----|
|  |
``` math
\sum_{i=1}^{N}\,\bigl(g_{i}\ast h_{i,k}\bigr)(s)=h_{r,k}(0)\,\delta(s).
``` |  | (65) |

By
Assumption [1](#Thmassm1 "Assumption 1. ‣ III-A MCLP for a Single Source ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")
and
([27](#S3.E27 "In Proposition 4. ‣ III-B MCLP for Multiple Sources ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")),
it follows that

|  |  |  |  |
|----|----|----|----|
|  |
``` math
\sum_{i=1}^{N}\,\bigl(g_{i}\ast h_{i,k}\bigr)(s)=c^{\prime}\,\exp(-j\,\omega\,\tau_{r,k})\,\delta(s),
``` |  | (66) |

where $`c^{\prime}`$  is independent of $`r`$  and $`k`$. Using
([22](#S3.E22 "In III-B MCLP for Multiple Sources ‣ III Multi-Channel Linear Prediction for Dereverberation ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")),
and the properties of convolution, we finally obtain

|  |  |  |  |  |
|----|----|----|----|----|
|  | $`\displaystyle\begin{split}\sum_{i=1}^{N}\,g_{i}(s)\ast y_{i}(s)&=\sum_{i=1}^{N}\,g_{i}(s)\ast\Biggl(\sum_{k=1}^{K}\,x_{k}(s)\ast h_{i,k}(s)\Biggr)\\
&=\sum_{k=1}^{K}\,x_{k}(s)\ast\sum_{i=1}^{N}\,g_{i}(s)\ast h_{i,k}(s)\\
&=\sum_{k=1}^{K}\,x_{k}(s)\ast\Bigl(c^{\prime}\,\exp(-j\,\omega\,\tau_{r,k})\,\delta(s)\Bigr)\\
&=c^{\prime}\,\sum_{k=1}^{K}\,x_{k}(s)\,\exp(-j\,\omega\,\tau_{r,k}).\end{split}`$ |  |  | (67) |

## Appendix E Motivation for the Post-Filter in Sec. [IV-C2](#S4.SS3.SSS2 "IV-C2 Post-Filter ‣ IV-C Source Separation ‣ IV Implementation Details ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")

In order to justify the variance expression in
([48](#S4.E48 "In IV-C2 Post-Filter ‣ IV-C Source Separation ‣ IV Implementation Details ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")),
we will make use of
Prop. [9](#Thmprop9 "Proposition 9. ‣ Appendix E Motivation for the Post-Filter in Sec. ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")
below. For simplicity, this proposition uses a seemingly independent
notation. We will identify the elements in the proposition with our
separation scenario right after the proposition. Recall that a complex
valued Gaussian random variable $`Z`$ is said to be circular
\[[19](#bib.bib19)\] and have variance $`2\,\sigma^{2}`$  if its real
and imaginary parts are independent and Gaussian random variables with
variance $`\sigma^{2}`$.

###### Proposition 9.

Suppose $`\gamma`$ is an unknown constant and we observe
$`Y_{n}=c_{n}\gamma+u_{n}\,Z_{n}`$, for $`n=1,\ldots,N`$, where
$`|c_{n}|=1`$, $`u_{n}\in\mathbb{C}`$ and $`Z_{n}`$’s are circularly
symmetric, independent, unit variance, zero-mean Gaussian random
variables.

The uniformly minimum variance unbiased (UMVU) estimator for $`\gamma`$
is of the form

|     |                                         |     |      |
|-----|-----------------------------------------|-----|------|
|     |
       ``` math
       \hat{\gamma}=\sum_{n=1}^{N}w_{n}\,Y_{n}
       ```                                      |     | (68) |

where

|     |                                       |     |      |
|-----|---------------------------------------|-----|------|
|     |
       ``` math
       w_{n}=c_{n}^{*}\,\beta\,|u_{n}|^{-2},
       ```                                    |     | (69) |

with

|     |                                                     |     |      |
|-----|-----------------------------------------------------|-----|------|
|     |
       ``` math
       \beta=\left(\sum_{n=1}^{N}|u_{n}|^{-2}\right)^{-1}.
       ```                                                  |     | (70) |

We also have that,

|  |  |  |  |
|----|----|----|----|
|  |
``` math
\frac{1}{N-1}\sum_{n=1}^{N}|w_{n}|\,\Bigl|Y_{n}-c_{n}\hat{\gamma}\Bigr|^{2}
``` |  | (71) |

is an unbiased estimator for the variance of $`\hat{\gamma}`$.

###### Proof (Sketch).

It can be shown, by taking into account the Gaussian pdf of $`Y_{n}`$,
that a complete sufficient statistic for $`\gamma`$ is

|     |                                                 |     |      |
|-----|-------------------------------------------------|-----|------|
|     |
       ``` math
       U=\sum_{n=1}^{N}c_{n}^{*}\,|u_{n}|^{-2}\,Y_{n}.
       ```                                              |     | (72) |

Observing that $`\mathbb{E}(U)=\beta^{-1}\,\gamma`$, it follows by the
Rao-Blackwell theorem that the UMVUE is as given in
([68](#A5.E68 "In Proposition 9. ‣ Appendix E Motivation for the Post-Filter in Sec. ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"))
for $`w_{n}`$ as in
([69](#A5.E69 "In Proposition 9. ‣ Appendix E Motivation for the Post-Filter in Sec. ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"))
and $`\beta`$ as in
([70](#A5.E70 "In Proposition 9. ‣ Appendix E Motivation for the Post-Filter in Sec. ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation")).
It can also be checked that the variance of $`\hat{\gamma}`$  is,

|  |  |  |  |
|----|----|----|----|
|  |
``` math
\var(\hat{\gamma})=\sum_{n=1}^{N}\,|w_{n}|^{2}\,|u_{n}|^{2}=\beta^{2}\,\sum_{n=1}^{N}\,|u_{n}|^{-2}=\beta.
``` |  | (73) |

Let us now set
$`\nu=\sum_{n=1}^{N}|w_{n}|\,\Bigl|\,\bigl(Y_{n}-c_{n}\hat{\gamma}\bigr)\Bigr|^{2}`$.
Then,

|  |  |  |  |  |
|----|----|----|----|----|
|  | $`\displaystyle\begin{split}&\mathbb{E}(\nu)=\sum_{n=1}^{N}|w_{n}|\,\mathbb{E}\Bigl(\bigl|Y_{n}-c_{n}\hat{\gamma}\bigr|^{2}\Bigr)\\
&=\sum_{n=1}^{N}|w_{n}|\,\left(\bigl(1-|w_{n}|\bigr)^{2}\,|u_{n}|^{2}+\sum_{k\neq n}|w_{k}|^{2}\,|u_{k}|^{2}\right)\\
&=\sum_{n=1}^{N}|w_{n}|\,\left(\bigl(1-|w_{n}|\bigr)^{2}\,|u_{n}|^{2}+\beta\sum_{k\neq n}|w_{k}|\right)\\
&=\sum_{n=1}^{N}|w_{n}|\,\left(\bigl(1-|w_{n}|\bigr)^{2}\,\frac{\beta}{|w_{n}|}+\beta(1-|w_{n}|)\right)\\
&=\sum_{n=1}^{N}\beta(1-|w_{n}|)\\
&=\beta(N-1).\end{split}`$ |  |  | (74) |

∎

In order to relate this proposition with the discussion in
Section [IV-C2](#S4.SS3.SSS2 "IV-C2 Post-Filter ‣ IV-C Source Separation ‣ IV Implementation Details ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"),
we make the following identifications :
$`{Y_{n}\to\hat{Y}_{n}(s,\omega)}`$; $`{c_{n}\to a_{\theta,n}}`$;
$`{w_{n}\to W_{i,n}}`$; $`{\hat{\gamma}\to\hat{S}_{i}(s,\omega)}`$.

For the $`i^{\text{th}}`$ source, if we regard all of the remaining
sources as noise terms with a Gaussian distribution, then the optimal
estimate of the $`i^{\text{th}}`$ source is obtained by a linear
combination of the observations. We assume that GSS approximately finds
these optimal weights. In that case, the expression in
([48](#S4.E48 "In IV-C2 Post-Filter ‣ IV-C Source Separation ‣ IV Implementation Details ‣ Multichannel Linear Prediction for Blind Reverberant Audio Source Separation"))
gives an estimate of the variance of these unwanted components (or,
‘noise’) that remain after GSS.

## References

- \[1\] İ. Bayram. A multichannel audio denoising formulation based on
  spectral sparsity. IEEE/ACM Transactions on Audio, Speech, and
  Language Processing, 23(12):2272–2285, Dec 2015.
- \[2\] J. Bitzer and K. U. Simmer. Superdirective microphone arrays. In
  M. Brandstein and D. Ward, editors, Microphone Arrays. Springer, 2001.
- \[3\] S. Boyd, N. Parikh, E. Chu, B. Peleato, and J. Eckstein.
  Distributed optimization and statistical learning via the alternating
  direction method of multipliers. Foundations and Trends in Machine
  Learning, 3(1):1–122, 2011.
- \[4\] D. H. Brandwood. A complex gradient operator and its application
  in adaptive array theory. Microwaves, Optics and Antennas, IEE
  Proceedings H, 130(1):11–16, February 1983.
- \[5\] P. L. Combettes and J.-C. Pesquet. Proximal splitting methods in
  signal processing. In H. H. Bauschke, R. S. Burachik, P. L. Combettes,
  V. Elser, D. R. Luke, and H. Wolkowicz, editors, Fixed-Point
  Algorithms for Inverse Problems in Science and Engineering. Springer,
  New York, 2011.
- \[6\] A. Farina. Simultaneous measurement of impulse response and
  distortion with a swept-sine technique. In Proc. 108th Convention of
  the AES, 2000.
- \[7\] S. Gannot and I. Cohen. Adaptive beamforming and postfiltering.
  In Handbook of Speech Processing. Springer, 2008.
- \[8\] F. R. Gantmacher. The Theory of Matrices - I. Chelsea Publishing
  Company, 1959.
- \[9\] A. Hyvärinen, J. Karhunen, and E. Oja. Independent Component
  Analysis. Wiley, 2001.
- \[10\] A. Jukić, T. van Waterschoot, T. Gerkmann, and S. Doclo.
  Multi-channel linear prediction-based speech dereverberation with
  sparse priors. IEEE/ACM Transactions on Audio, Speech, and Language
  Processing, 23(9):1509–1520, Sept 2015.
- \[11\] A. Jukić, T. van Waterschoot, T. Gerkmann, and S. Doclo. A
  general framework for incorporating time - frequency domain sparsity
  in multichannel speech dereverberation.  J. Audio Eng. Soc,
  65(1/2):17–30, 2017.
- \[12\] K. Kinoshita, M. Delcroix, T. Nakatani, and M. Miyoshi.
  Suppression of late reverberation effect on speech signal using
  long-term multiple-step linear prediction. IEEE Transactions on Audio,
  Speech, and Language Processing, 17(4):534–545, May 2009.
- \[13\] M. Kowalski, E. Vincent, and R. Gribonval. Beyond the
  narrowband approximation: Wideband convex methods for under-determined
  reverberant audio source separation. IEEE Trans. Audio, Speech and
  Language Proc., 18(7):1818–1829, Sept 2010.
- \[14\] P. L. Lions and B. Mercier. Splitting algorithms for the sum of
  two nonlinear operators. SIAM Journal on Numerical Analysis,
  16(6):964–979, 1979.
- \[15\] M. Miyoshi and Y. Kaneda. Inverse filtering of room acoustics.
  IEEE Transactions on Acoustics, Speech, and Signal Processing,
  36(2):145–152, Feb 1988.
- \[16\] T. Nakatani, T. Yoshioka, K. Kinoshita, M. Miyoshi, and B. H.
  Juang. Speech dereverberation based on variance-normalized delayed
  linear prediction. IEEE Transactions on Audio, Speech, and Language
  Processing, 18(7):1717–1731, Sept 2010.
- \[17\] P. A. Naylor, E. A. P. Habets, N. D. Gaubitch, J. Y.-C. Wen,
  M. R. P. Thomas, S. Gannot, A. W. H. Khong, J. Hopgood, C. Evers,
  S. Fortune, M. Miyoshi, J. Buchner, and W. Kellermann. Speech
  Dereverberation. Springer, 2010.
- \[18\] L. C. Parra and C. V. Alvino. Geometric source separation:
  Merging convolutive source separation with geometric beamforming. IEEE
  Transactions on Speech and Audio Processing, 10(6):352–362, Sep 2002.
- \[19\] B. Picinbono. On circularity. IEEE Transactions on Signal
  Processing, 42(12):3473–3482, Dec 1994.
- \[20\] J. P. Reilly, M. Wilbur, M. Seibert, and N. Ahmadvand. The
  complex subband decomposition and its application to the decimation of
  large adaptive filtering problems. IEEE Trans. Signal Processing,
  50(11):2730–2743, Nov 2002.
- \[21\] R. Roy and T. Kailath. ESPRIT-Estimation of signal parameters
  via rotational invariance techniques. IEEE Transactions on Acoustics,
  Speech, and Signal Processing, 37(7):984–995, Jul 1989.
- \[22\] R. Schmidt. Multiple emitter location and signal parameter
  estimation. IEEE Transactions on Antennas and Propagation,
  34(3):276–280, Mar 1986.
- \[23\] K. U. Simmer, J. Bitzer, and C. Marro. Post-filtering
  techniques. In M. Brandstein and D. Ward, editors, Microphone Arrays.
  Springer, 2001.
- \[24\] M. Souden, S. Araki, K. Kinoshita, T. Nakatani, and H. Sawada.
  A multichanneMMSE-based framework for speech source separation and
  noise reduction. IEEE Transactions on Audio, Speech, and Language
  Processing, 21(9):1913–1928, Sept 2013.
- \[25\] G. Su and M. Morf. The signal subspace approach for multiple
  wide-band emitter location. IEEE Transactions on Acoustics, Speech,
  and Signal Processing, 31(6):1502–1522, Dec 1983.
- \[26\] M. Togami, Y. Kawaguchi, R. Takeda, Y. Obuchi, and N. Nukaga.
  Optimized speech dereverberation from probabilistic perspective for
  time varying acoustic transfer function. IEEE Transactions on Audio,
  Speech, and Language Processing, 21(7):1369–1380, July 2013.
- \[27\] J. M. Valin, J. Rouat, and F. Michaud. Enhanced robot audition
  based on microphone array source separation with post-filter. In 2004
  IEEE/RSJ International Conference on Intelligent Robots and Systems
  (IROS) (IEEE Cat. No.04CH37566), volume 3, pages 2123–2128 vol.3,
  Sept 2004.
- \[28\] J.-M. Valin, J. Rouat, and F. Michaud. Microphone array
  post-filter for separation of simultaneous non-stationary sources. In
  Proc. IEEE Int. Conf. on Acoustics, Speech and Signal Proc.
  (ICASSP), 2004.
- \[29\] E. Vincent, R. Gribonval, and C. Fevotte. Performance
  measurement in blind audio source separation. IEEE Transactions on
  Audio, Speech, and Language Processing, 14(4):1462–1469, July 2006.
- \[30\] M. Wax, Tie-Jun Shan, and T. Kailath. Spatio-temporal spectral
  analysis by eigenstructure methods. IEEE Transactions on Acoustics,
  Speech, and Signal Processing, 32(4):817–827, Aug 1984.
- \[31\] O. Yilmaz and S. Rickard. Blind separation of speech mixtures
  via time-frequency masking. IEEE Transactions on Signal Processing,
  52(7):1830–1847, July 2004.
- \[32\] Y.-S. Yoon, L. M. Kaplan, and J. H. McClellan. TOPS: New DOA
  estimator for wideband signals. IEEE Transactions on Signal
  Processing, 54(6):1977–1989, June 2006.
````
